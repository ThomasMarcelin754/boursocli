# Secrets — never commit (config.json holds bearer/cookies, 0600)
config.json
*.config.json
.env

# Built binary
/boursocli
/cmd/boursocli/boursocli

# goreleaser output (binaries, archives, checksums, SBOMs)
/dist/

# Infra / session-measurement tooling (manipulates live cookies, logs contain secrets)
scripts/measure-session-ttl.sh
scripts/session-ttl.log

# Go
*.test
*.out

# Node (sweet-cookie runtime cache)
node_modules/

# macOS
.DS_Store

# Local Claude Code tooling config — machine-specific, never public
.claude/
